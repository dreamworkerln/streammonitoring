package ru.kvanttelecom.tv.videomonitoring.utils.dto;

import lombok.Data;

/**
 * CameraEventDto data - new stream has been created
 */
@Data
public class CameraEventDataCreateDto extends CameraEventData {

    private CameraStatusDto stream;

    public CameraEventDataCreateDto() {}

    public CameraEventDataCreateDto(CameraStatusDto stream) {
        this.stream = stream;
    }
}
